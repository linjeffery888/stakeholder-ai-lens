# Copy this file to `.env` (which is git-ignored) and paste your key below.
# When a key is present, Lens runs in LIVE mode automatically (real Claude
# extraction + semantic dedup). No key -> deterministic offline mock mode.
#
#   cp .env.example .env      # then edit .env and add your key
#
ANTHROPIC_API_KEY=sk-ant-...
